pipeline{
    agent any
    stages{
        stage("Build"){ 
            when {
                allOf {
                    expression {
                        return currentBuild() == 4
                    }
                    environment ignoreCase: true, name: 'branch', value: 'dev'
                }
                anyOf {
                    branch "master";
                    branch "dev"
                }
            }
            steps{
                sh "sh ./scripts/build.sh"
                sh "echo 'this is on dev branch'"
            }
        }
        stage("Deliver"){
            steps{
                sh "sh ./scripts/run.sh"
                sh "echo 'added auto trigger'"
            }
        }
    }
}